#!/bin/bash

# Замена всех пробелов на символы табуляции в файле sequences.txt
sed -i 's/ /\t/g' sequences.txt

# Вывод сообщения об успешном завершении операции
echo "Замена пробелов на табуляцию в sequences.txt выполнена успешно."
