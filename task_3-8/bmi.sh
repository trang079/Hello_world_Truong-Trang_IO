#!/bin/bash

# Запрос данных у пользователя
read -p "Введите массу тела (кг): " WEIGHT
read -p "Введите рост (метры, например 1.75): " HEIGHT

# Расчет ИМТ
BMI=$(( $(echo "$WEIGHT / ($HEIGHT * $HEIGHT)" | bc -l) / 1 ))

# Вывод результата
echo "Индекс массы тела (ИМТ) — $BMI"