# Поиск суммы всех чётных чисел от 1 до 15 с помощью цикла for
sum_even = 0

for i in range(1, 16):
    if i % 2 == 0:
        sum_even = sum_even + i

print("Сумма чётных чисел (for):", sum_even)
